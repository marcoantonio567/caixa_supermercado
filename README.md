# Caixa Supermercado

Projeto de controle de caixa e estoque. Leia `LEIAME.md` para instruções gerais de instalação.

## Executando os testes

1. Instale as dependências necessárias:
   ```bash
   pip install pytest pandas openpyxl
   ```
2. Na raiz do projeto, execute:
   ```bash
   pytest
   ```
